/**
 * 제안 모달 너비 (루트 1rem = 16px 기준, px 고정).
 * 프로퍼티 패널·Objects 서브모달 축(S)과 정합.
 */
export const MODAL_WIDTH_SM_PX = 320;
export const MODAL_WIDTH_MD_PX = 480;
export const MODAL_WIDTH_LG_PX = 720;

/** 대형 모달 본문 스크롤 상한 (뷰포트 대비) */
export const MODAL_EXAMPLE_L_MAX_BODY_VH = 78;
